allprojects {
    group = 'com.example'
    version = '1.0.0'
    repositories { mavenCentral() }
}

subprojects {
    plugins.withId('java') {
        tasks.withType(Test).configureEach {
            useJUnitPlatform()
        }
    }
}
